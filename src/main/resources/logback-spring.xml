<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logging configuration for the BABA LAGIN application.

  Execution logs are written to rolling files under ./logs so they survive
  restarts (important for a live deployment):
    - logs/babalagin.log        : everything at INFO and above
    - logs/babalagin-error.log  : only WARN/ERROR (quick incident triage)

  Files roll daily and at 10 MB, keep 30 days of history, and are capped at
  500 MB total. Each line carries a per-request correlation id ([%X{requestId}])
  set by RequestLoggingFilter, so all logs for one HTTP call can be grepped
  together.
-->
<configuration>

    <!-- Registers Spring Boot's conversion words (%clr, %wEx) and base properties. -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Log directory can be overridden with -Dlog.dir=/var/log/babalagin -->
    <property name="LOG_DIR" value="${log.dir:-logs}"/>
    <property name="APP" value="babalagin"/>

    <property name="CONSOLE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %clr(%-5level) [%X{requestId:-startup}] %clr(%logger{36}){cyan} - %msg%n"/>
    <property name="FILE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{requestId:-startup}] %logger{40} - %msg%n"/>

    <!-- Console (for dev / when run in a terminal) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Main rolling file: INFO and above -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${APP}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${APP}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Error-only rolling file for fast incident triage -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${APP}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${APP}-error-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Our application code: DEBUG to file, so detailed traces are captured -->
    <logger name="com.example.login" level="DEBUG"/>

    <!-- Trim third-party noise -->
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.springframework" level="INFO"/>
    <logger name="com.zaxxer.hikari" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

</configuration>
